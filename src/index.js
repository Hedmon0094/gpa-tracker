import React from 'react';
import ReactDOM from 'react-dom/client';
import GPACalculator from './App';
import reportWebVitals from './reportWebVitals';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GPACalculator />
   </React.StrictMode>
);


reportWebVitals();
